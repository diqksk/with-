어레이리스트에 만들어오기


배열을 공부해야하는 시간의 2배 3배 이상을 컬렉션공부에 투자해야한다.


*CRUD

(입력 출력 검색 수정 삭제)


자바의 예외처리

 sysntex - 컴파일도 안되는것 - >컴파일 에러
 실행중오동작 - > 런타임에러 ->Exception


예외발생경우
정수를 0으로 나누는 경우
배열의 크기보다 큰 인덱스로 배열의 원소를 접근하는 경우
정수를 읽는 코드가 실행중일때 문자를 입력한 경우


자바의 예외처리

try-catch-finally 문 사용

try{
예외가 발생할 가능성이 있는 실행문
}
catch(처리할 예외 타입 선언){
예외처리문
}
finally{
예외 발생 여부와 상관없이 무조건 실행되는 문장(finally 블록}   <<=== 생략가능
}

-----------------------------------------------
패키지 개념과 자바 패키지 활용
역공학 - 완성품을 먼저 보고 그다음 설계를 함.{되는대로 막 만들어보고 그다음 전체를 다시보고 다시 설계함}(reverse Engineering)
다른사람들이 쓸수있게 만들어있는거 - 라이브러리

객체지향은 다형성때문에 배우는것 (캡슐화,상속,다형성) 3요소

날짜와 시간관리 / String StringBuffer <<<--------- 시간을 좀 투자해 익숙해져야하는 애들이다.

디폴트패키지에는 클래스를 왠만하면 만들지 마라(패키지도 뎁스를 최소 3개이상은 줘야 충돌을 방지하기 좋음)


abstract가 있는 메소드는 추상메소드(블록이없음) 는 반드시 상ㅇ속받는 자식메소드에서 블록을 만들어줘야함.

* 생성자는 클래스와 이름이 같아야하고 리턴형이 반드시 없어야한다.!!

참조값이 같은가를 비교하는건 얕은비교

값안에 있는 객체들이 같은지 비교하는건 깊은비교



예제 6-3 6-4 만들어보기!!!





Wrapper 클래스 - 기본타입 8개의 클래스를 넣어놓은 클래스 ( String은 그냥 기본처럼..)
-특징 auto boxing이 있다 : 생성자를 생성하지 않고 사용할 수 있다.
boxing : 기본타입의 값을 Warpper객체로 변환하는것
unboxing : Wrapper객체에 들어있는 기본타입의 값을 빼내는것



String 클래스
String리터럴은 String 객체로 처리됨.
오토박싱이 됨

컬렉션과 제네릭
Set(중복이안되도록 저장) , Map(key(문자열가능):value)가 한쌍으로 접근함 , List(배열과 비슷하다(index를 이용해 접근))<<-개념만 이해하고있으면 쉽게 컬렉션 사용 가능

Queue -> 선입선출
Stack -> 후입선출

컬랙션의 요소는 '객체'만 가능


iterator ->열거자( set에있는거 다

